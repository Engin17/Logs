<Window x:Class="Logs.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Logs.ViewModels"
        xmlns:local="clr-namespace:Logs"
        mc:Ignorable="d"
        Title="SeeTec Logs" Background="Gray" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="100" />
            <RowDefinition Height="70" />
            <RowDefinition Height="250" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="150" />
        </Grid.ColumnDefinitions>

        <Button Content="Server logs" Height="50" Width="80" Grid.Row="0" Grid.Column="5" Command="{Binding ExportServerLogsCommand}" IsEnabled="{Binding IsBtnServerLogsEnabled}"/>
        <Button Content="Client logs" Height="50" Width="80" Grid.Row="0" Grid.Column="0" Command="{Binding ExportClientLogsConfCommand}" IsEnabled="{Binding IsBtnClientLogsConfEnabled}"/>
        <Button Height="50" Width="80" Grid.Row="0" Grid.Column="2" Command="{Binding OpenLogsPathCommand}">
            <TextBlock TextAlignment="Center">Open logs<LineBreak /> zip path</TextBlock>
        </Button>

        <Button Height="50" Width="80" Grid.Row="1" Grid.Column="5" IsEnabled="{Binding IsBtnServerFTPEnabled}"  Command="{Binding UploadServerFilesFTPCommand}">
            <TextBlock>Server logs <LineBreak /> FTP-Server</TextBlock>
        </Button>
        <Button Height="50" Width="80" Grid.Row="1" Grid.Column="0" IsEnabled="{Binding IsBtnClientFTPEnabled}" Command="{Binding UploadClientFilesFTPCommand}">
            <TextBlock>Client logs <LineBreak /> FTP-Server</TextBlock>
        </Button>
        <Button Height="50" Width="80" Grid.Row="1" Grid.Column="2" IsEnabled="{Binding IsBtnUploadAllFTPEnabled}" Command="{Binding UploadAllFilesFTPCommand}">
            <TextBlock>Upload all<LineBreak /> logs to FTP</TextBlock>
        </Button>

        <StackPanel Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3">
            <ProgressBar Height="20" Width="300"  IsIndeterminate="true" Margin="5,5,5,0" Visibility="{Binding ProgressbarVisibility}"/>
            <TextBox Width="180" TextAlignment="Center" Background="Gray" BorderThickness="0" Text="{Binding TbProgressText}" Visibility="{Binding TbProgressTextEnabled}"/>
        </StackPanel>
        
        <TextBox Background="White" Grid.Row="4" Grid.ColumnSpan="5" Text="{Binding LogText}" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto" BorderThickness="2" FontSize="13" />

    </Grid>
</Window>
